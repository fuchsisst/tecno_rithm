import 'package:flutter/material.dart';

class CardServices extends StatelessWidget {
  const CardServices({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(

      ),
    );
  }
}
