import 'package:flutter/material.dart';

class MainSmartphonePage extends StatelessWidget {
  const MainSmartphonePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
